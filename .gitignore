node_modules/
dist/
*.local
.DS_Store
# Machine-written verification evidence. Regenerated by `npm test`; never
# hand-edited, and never committed — a checked-in test count is a claim about
# a run that may not have happened.
.verify/
# Screenshots from `npm run capture`. Regenerated on demand; committing them
# would put megabytes of build state in history for no gain.
.capture/
